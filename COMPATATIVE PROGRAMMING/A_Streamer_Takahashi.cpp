#include <bits/stdc++.h>
using namespace std ;
int main (){
    int l , r ,n ; // l is initial time and r is last time , n is listeners ;
    cin>> l >> r >>n ;
    for (int i = 1 ;i <= n ;i++){
        int x , y ;
        cin>> x >> y ;
        if (l <= x && r  )
    }
    if ( l <= x && r <= y  ){
        cout<< n ;
    }
    cout<< 

        return 0 ;
}

